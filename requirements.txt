PyMuPDF>=1.24,<2
python-docx>=1.1,<2
pyinstaller>=6.0,<7
pywinauto>=0.6.9,<0.7
# Optional: OCR-assisted result-row suggestion (result_ocr.py). Uses the
# Windows-native OCR engine (no extra binary/model bundled). If this or the
# grid calibration points are missing at runtime, the app silently falls
# back to the existing pure-manual selection flow.
Pillow>=10,<12
winocr>=0.0.15,<0.1
